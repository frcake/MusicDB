<div class="container">
  <div class="col-md-5 col-md-offset-4" style="margin-bottom: 5%">
    <h1><%= @category.name%></h1>
  </div>
  <% @albums.each do |album|%>
    <%= render 'albums/local_index', album: album%>
  <%end%>
</div>
